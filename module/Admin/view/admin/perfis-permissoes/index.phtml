<h2>Perfis Permissões</h2>
<div class="row">
    <?php
        if (sizeof($this->menuButtons))
            $this->menuBar($this->menuButtons);
    ?>
</div>

<div class="row">
    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <table class="table table-hover table-condensed">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Módulo</th>
                    <th>Perfil</th>
                    <th>Descrição do perfil</th>
                    <th style="width:135px; text-align: center;">Ação</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($this->perfis as $key => $value) : ?>
                    <tr>
                        <td><?php echo $value->prf_id; ?></td>
                        <td><?php echo $value->mod_nome; ?></td>
                        <td><?php echo $value->prf_nome; ?></td>
                        <td><?php echo $value->prf_descricao; ?></td>
                        <td>
                            <a class='btn btn-success' href='#' data-id="<?php echo $value->prf_id; ?>">
                                <i class='glyphicon glyphicon-ok'></i> Atribuir Permissões
                            </a>
                        </td>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

<script type="text/javascript">
    $('.btn-success').click(function(e) {
        var itemSelecionado = $(e.currentTarget).data('id');
        ids = [itemSelecionado];
        goToAction('perfispermissoes','atribuirpermissoesperfil',ids);
    });
</script>
